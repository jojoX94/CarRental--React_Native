// create a location model with the following properties: latitude, longitude and label name in interface Location
//
// Path: src/models/locationModel.ts
interface LocationModel {
  latitude: number;
  longitude: number;
  label: string;
}

export default LocationModel;
